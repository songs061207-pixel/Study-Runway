# TODO.md

## 下一步优先级

1. 用浏览器实际打开页面，检查学习库、学习项详情、今日任务、Roadmap、周计划、趋势页的 UI 是否顺畅。
2. 检查用户当前真实 `localStorage` 数据，确认 Phase 0、物理课 Phase 1、deadline 校准是否按预期迁移。
3. 给学习库增加更细的编辑能力，例如直接编辑 Phase、Track、依赖、deadline、scheduleMode，而不只靠弹窗。
4. 给 Roadmap 页面增加更清晰的 Phase 0 展示，让用户能看出它是轻学习启动层。
5. 给学习资料详情页增加批量创建 unit 的入口，方便把 PDF 章节、纸质资料页段一次录入。
6. 增加导入/导出功能，避免浏览器 `localStorage` 数据丢失。
7. 给 deadline 校准按钮增加预览模式：先展示会改哪些学习项，再确认覆盖。
8. 给 reference 资料增加更显眼的 companion resource 展示和手动挂载关系。
9. 增加基础单元测试或调度器测试，覆盖依赖锁定、Phase 0 轻槽、reference 不入槽、长 unit 拆分。
10. 考虑用 dynamic import 拆分页面 chunk，解决 Vite 500KB 体积警告。

## 需要谨慎处理

- 不要自动删除用户学习记录。
- 不要自动把“记录了足够分钟”改成“已完成”。
- 不要让 Phase 0 挤占重学习槽。
- 不要让 reference 资料单独进入今日任务。
- 不要在用户未确认时重复覆盖 deadline。
- 不要把 `/courses` 路由直接改名成 `/library`，除非同时做兼容跳转。

## 可讨论方向

- 是否需要“学期模式”：按开学/考试/假期生成节奏。
- 是否需要“精力时间轴”：上午重课、下午练习、晚上轻课。
- 是否需要“学习目标模板”：算法主线、AI/ML 主线、系统编程主线、项目实践主线。
- 是否需要“掌握度反馈”：完成 unit 后给理解评分，影响复习和后续排课。
- 是否需要“资源关系图”：课程、书籍、题单、reference 之间可视化依赖。

 